# wasm builds: getrandom 0.3 (via simple-bte -> rand 0.9) requires an explicit
# backend selection on wasm32-unknown-unknown.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
