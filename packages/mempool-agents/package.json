{
  "name": "bte-mempool-agents",
  "version": "0.0.0",
  "private": true,
  "description": "On-chain agents for the encrypted-mempool demo: relayer (sponsors visitor swaps), searcher (real sandwich bot), settler (opens the sealed batch on-chain).",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "relayer": "tsx src/relayer.ts",
    "searcher": "tsx src/searcher.ts",
    "settler": "tsx src/settler.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bte-sdk": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.3",
    "@types/node": "^22.0.0"
  }
}
